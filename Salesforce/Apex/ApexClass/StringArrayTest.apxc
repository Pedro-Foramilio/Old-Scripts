public class StringArrayTest {
    public static List<String> generateStringArray(Integer len) {
        List<String> result = new List<String>();
        for (Integer i = 0; i < len; i++) {
            result.add('Test ' + i);
            System.debug('Element added: ' + i);
        }
        
        return result;
    }

}
